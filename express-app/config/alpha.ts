/** 数据库连接对象 */
const sqlConnection = {
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'covid19-visualization'
}

/** 测试版配置 */
const config = { sqlConnection }

export default config
